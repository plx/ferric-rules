# Phase 005 Progress

## Baseline
- 1055 tests passing at start of Phase 5
- clippy clean, cargo check --workspace clean
- Branch: experiments/phases/005

## Passes

### Pass 001: Phase 5 Baseline and Harness Alignment — COMPLETE
- Documented Phase 5 baseline assumptions in ferric-ffi/src/lib.rs and ferric-cli/src/main.rs
- Created FFI test helpers (crates/ferric-ffi/src/tests/test_helpers.rs) with fixture path resolution and assertions
- Created CLI test helpers (crates/ferric-cli/tests/cli_helpers.rs) with binary invocation, exit code, and output helpers
- Created FFI fixture skeletons: simple_rule.clp, assert_retract.clp, module_visibility.clp, parse_error.clp, generic_dispatch.clp
- Created CLI fixture skeletons: hello.clp, check_valid.clp, check_invalid.clp, multi_module.clp
- Created CLI integration test (crates/ferric-cli/tests/cli_integration.rs) with 8 scaffold tests
- Fixed pre-existing issue: debug_assert_consistency methods in ferric-runtime needed #[cfg(any(test, debug_assertions))] gate for ffi-release profile compatibility
- Phase 4 baseline confirmed green (1055 tests)

### Pass 002: Workspace Profiles and Crate Scaffolding — COMPLETE
- Added ferric-ffi and ferric-cli to workspace members and dependencies
- Created ffi-dev and ffi-release profiles with panic = "abort"
- Scaffolded ferric-ffi: Cargo.toml (cdylib+staticlib), src/lib.rs, error.rs, engine.rs, types.rs, header.rs, tests/ module
- Scaffolded ferric-cli: Cargo.toml, src/main.rs with command dispatch, commands/{run,check,repl,version}.rs stubs
- ferric-ffi uses per-crate lint override for unsafe_code = "allow"
- All profiles compile: dev, ffi-dev, ffi-release
- Test count: 1066 (1055 baseline + 3 FFI + 8 CLI)

### Pass 003: FFI Error Model and Unified Return Convention — COMPLETE
- Defined #[repr(C)] FerricError enum with 11 stable numeric codes (Ok=0 through InternalError=99)
- Implemented thread-local global error storage (set/clear/with_global_error)
- Implemented per-engine EngineErrorState (set/clear/message)
- Implemented error mapping: EngineError -> FerricError, LoadError -> FerricError
- Implemented C API: ferric_last_error_global() returns *const c_char, ferric_clear_error_global()
- Implemented convenience mappers: set_engine_error_global, set_load_error_global
- Created FerricEngine opaque handle struct (engine + error_state)
- 19 error model tests covering: code distinctness, global/engine channel isolation, C API pointer behavior, error mapping for all variants
- Test count: 1085 (1066 + 19 error model tests)

### Pass 004: FFI Thread Affinity and Engine Lifecycle APIs — COMPLETE
- Made Engine::check_thread_affinity pub (was pub(crate)) for FFI access
- Implemented internal helpers: validate_engine_ptr, validate_engine_ptr_mut, check_thread_affinity
- Canonical two-step borrow pattern: shared ref for thread check → mutable ref after validation
- Implemented C API: ferric_engine_new, ferric_engine_free, ferric_engine_load_string, ferric_engine_reset
- Implemented C API: ferric_engine_last_error, ferric_engine_clear_error (per-engine error channel)
- Thread violation returns FerricError::ThreadViolation without panicking (FFI-safe, no debug_assert)
- Cross-thread violation test using raw pointer transfer (usize → *mut roundtrip)
- 15 lifecycle tests: null safety, load valid/invalid, error channels, thread affinity, multi-engine independence
- Test count: 1100 (1085 + 15 lifecycle tests)

### Pass 005: FFI Core run/step/assert/retract APIs — COMPLETE
- Implemented ferric_engine_run with limit semantics (-1 = unlimited, positive = count)
- Implemented ferric_engine_step with status signaling (1=fired, 0=empty, -1=halted)
- Implemented ferric_engine_assert_string via load_str for full CLIPS form parsing
- Implemented ferric_engine_retract with FactId↔u64 conversion via slotmap::KeyData
- Implemented ferric_engine_get_output for channel-based output retrieval
- Added slotmap dependency to ferric-ffi for FactId conversion
- 18 execution tests: run/step/assert/retract with null safety, limits, output verification
- Test count: 1118 (1100 + 18 execution tests)

### Pass 006: FFI Copy-To-Buffer Error APIs and Edge Cases — COMPLETE
- Implemented `copy_error_to_buffer` shared helper in error.rs (pub(crate))
- Implemented `ferric_last_error_global_copy` C API: buf/buf_len/out_len contract
- Implemented `ferric_engine_last_error_copy` C API: delegates to shared helper
- Full branch matrix: no-error (NotFound), size-query (Ok), exact-fit (Ok), truncation (BufferTooSmall), zero-len, one-byte, null-buf-nonzero-len (InvalidArgument)
- 16 copy-error tests: 10 global + 6 per-engine, covering every documented contract branch
- Test count: 1134 (1118 + 16 copy-error tests)

### Pass 007: FFI Extended API Value and Query Surface — COMPLETE
- Added `Engine::resolve_symbol` public method to ferric-runtime (no thread check — read-only)
- Implemented FerricValueType enum (#[repr(C)]) with 7 discriminants
- Implemented FerricValue struct (#[repr(C)]) with tagged fields and void() constructor
- Implemented value_to_ferric conversion: Value → FerricValue with heap-allocated strings/multifields
- Implemented resource management: ferric_string_free, ferric_value_free (recursive), ferric_value_array_free
- Implemented query APIs: ferric_engine_fact_count, ferric_engine_get_fact_field_count, ferric_engine_get_fact_field
- Implemented ferric_engine_get_global for global variable access with module visibility resolution
- 22 value/query tests: type conversion, resource management, fact queries, global queries, round-trip cycle
- Test count: 1156 (1134 + 22 value/query tests)

### Pass 008: C Header Generation, Thread-Safety Banner, and Ownership Docs — COMPLETE
- Added cbindgen 0.29 to workspace dependencies
- Created cbindgen.toml with C language, FERRIC_H guard, ScreamingSnakeCase enum prefix
- Created build.rs that generates ferric.h with thread-safety and ownership preamble
- Generated and committed ferric.h (418 lines) with all 20 FFI functions, 3 types, opaque FerricEngine
- Thread-safety banner documenting thread affinity and violation behavior
- Ownership/lifetime docs covering all pointer categories (borrowed, owned, external)
- 12 header smoke tests verifying symbols, banners, enum variants, struct fields, function declarations
- Test count: 1168 (1156 + 12 header tests)

### Pass 009: FFI Artifact Build Matrix and Panic Policy Verification — COMPLETE
- Verified ffi-dev and ffi-release profile builds succeed (cdylib + staticlib artifacts)
- 5 build matrix tests: profile builds, artifact existence, unwind verification
- 4 tests #[ignore]d (subprocess cargo builds, slow, for CI)
- 1 non-ignored test proves default test profile uses unwind (not abort)
- Added build instructions to crate-level docs (profiles, artifact table, panic policy)
- Test count: 1169 (1168 + 1 non-ignored build_matrix test; 4 additional ignored tests)

### Pass 010: CLI run/check/version Commands and Diagnostics — COMPLETE
- Implemented `ferric run`: load_file → reset → run → print output from channel "t"
- Implemented `ferric check`: load_file only, report errors to stderr
- Both commands: file-not-found → exit 1, parse/load errors → exit 1 with diagnostics
- Fixed latent bug: module-qualified rule names (e.g., `MAIN::start`) now associate with declared module
- 8 new CLI integration tests: run hello/multi-module, run/check missing/invalid files, -V flag
- Test count: 1177 (1169 + 8 CLI integration tests)

### Pass 011: REPL Interactive Loop and Command Surface — COMPLETE
- Added rustyline 17 to workspace dependencies
- Full REPL: line editing, history, multiline input with balanced-paren continuation
- REPL commands: (reset), (run [N]), (facts), (agenda), (clear), (exit)/(quit), (load "file")
- CLIPS forms evaluated via engine.load_str(), output from channel "t" printed after each form
- parens_balanced with string-aware and comment-aware tracking
- print_facts with ordered/template fact rendering using engine.resolve_symbol()
- 10 unit tests for paren balancing, 5 integration tests for REPL commands via piped stdin
- Test count: 1192 (1177 + 10 REPL unit + 5 REPL integration)

### Pass 012: Phase 5 Integration and Exit Validation — COMPLETE
- 6 FFI diagnostic parity tests: parse/compile errors, end-to-end cycle, fact mutation, error retrieval, copy-to-buffer retry
- 2 CLI diagnostic parity tests: run/check invalid source show diagnostic prefixes
- All quality gates pass:
  - cargo fmt --all --check: clean
  - cargo clippy --workspace --all-targets -- -D warnings: clean
  - cargo test --workspace: 1200 tests passing
  - cargo check --workspace --all-targets: clean
  - cargo build -p ferric-ffi --profile ffi-dev: clean
  - cargo build -p ferric-ffi --profile ffi-release: clean
- Test count: 1200 (1192 + 6 FFI diagnostic + 2 CLI diagnostic)

## Phase 5 Exit Summary

### Test Growth
- Phase 4 baseline: 1055 tests
- Phase 5 final: 1200 tests (+145)
- FFI tests: 113 (error model + lifecycle + execution + copy-error + values + header + build_matrix + diagnostic_parity)
- CLI tests: 31 (scaffold + integration + REPL unit + REPL integration + diagnostic parity)

### Definition of Done Checklist
1. ✓ C programs can create/embed and drive Ferric through a documented C API (20 FFI functions)
2. ✓ Unified FFI error handling across thread-local and per-engine channels
3. ✓ Copy-to-buffer error APIs with exact size/query/truncation contract
4. ✓ Thread-affinity checks enforced on every ferric_engine_* entry point
5. ✓ FFI panic policy enforced through ffi-dev/ffi-release profiles (panic=abort)
6. ✓ Generated ferric.h with thread-safety warning and ownership documentation
7. ✓ CLI commands (run, check, repl, version) with documented exit-code behavior
8. ✓ REPL with multiline input, commands, and source-located diagnostics
9. ✓ Phase 4 diagnostic contracts preserved through FFI and CLI
