{
  "name": "ferric-rules-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "vite preview --strictPort --base /ferric-rules",
    "check": "astro check",
    "typecheck": "tsgo --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "spellcheck": "cspell . --no-progress",
    "lint": "npm run format:check && npm run check && npm run typecheck && npm run spellcheck",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "validate": "npm run lint && npm run build && npm run test"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "cspell": "^10.0.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.0",
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
