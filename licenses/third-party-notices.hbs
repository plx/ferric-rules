# Rust Third-party License Notices

This file is generated by `scripts/license-notices.sh` using `cargo-about 0.9.0`.
Do not edit it by hand. It includes Ferric workspace crates as well as
third-party Cargo crates; Ferric workspace crates are licensed under
`MIT OR Apache-2.0`.

The notices below are generated from the Cargo workspace with:

```sh
cargo about generate --config about.toml --workspace --all-features --locked --offline --fail licenses/third-party-notices.hbs
```

For dependencies with an `OR` SPDX expression, `about.toml` controls which
acceptable license is selected for notice generation.

## License Overview

{{#each overview}}
- {{name}}: {{count}}
{{/each}}

{{#each licenses}}
## {{name}} (`{{id}}`)

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.license}} (`{{crate.license}}`){{/if}}{{#if crate.repository}} - {{crate.repository}}{{/if}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
