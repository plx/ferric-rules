/**
 * Re-export the built package for test convenience.
 *
 * All test files should import from this module rather than using
 * deep relative paths to the dist directory.
 */
export * from "../../dist/index";
